openapi: 3.1.0
info:
  title: SAP MARA
  description: SAP 料號資料
  version: '2.3'
servers:
  - url: https://218.35.174.245
paths:
  /api/MARA/GetMaterialJson:
    get:
      responses:
        200:
          description: '200 response description'
          content:
            application/json:
              schema: 
                type: array 
                items:
                  type: object
                  properties:
                    MATNR:                    
                      type: string
                    MAKTX:                    
                      type: string